# Hard Drive Cleanup Report
**Folder analyzed:** Projectmanagement  
**Date:** April 8, 2026  
**Total size:** 4.1 GB

---

## Summary

Your Projectmanagement folder has 4.1 GB of data, but **~3.5 GB can safely be reclaimed** by clearing out temporary build artifacts, stale agent worktrees, and redundant dependencies — without losing any of your actual work.

---

## 🔴 High Priority — Big Wins

### 1. Stale Git Worktrees — 1.4 GB
**Path:** `.claude/worktrees/`  
**Count:** 107 temporary worktrees left over from past agent runs

These are isolated working copies created by Claude Flow agents and are no longer needed. They contain duplicated files including full copies of `node_modules` (two of them are 143 MB and 142 MB each), interview prep files, logs, and more.

**What to do:** Delete the entire `.claude/worktrees/` folder.  
**Command:** `rm -rf Projectmanagement/.claude/worktrees/`  
**Space recovered: ~1.4 GB**

---

### 2. Node Modules — ~2 GB
**Paths:**
- `claude-flow/node_modules/` — 1.1 GB
- `claude-flow/v3/node_modules/` — 613 MB
- `claude-flow/node_modules/agentic-flow/node_modules/` — 250 MB (nested)

These can all be fully regenerated by running `npm install`. Notably, they include native runtime binaries for **Windows, Linux, and macOS** even though you're on a Mac — that alone wastes hundreds of MB.

**What to do:** Delete node_modules, reinstall only when needed.  
**Commands:**
```bash
rm -rf claude-flow/node_modules
rm -rf claude-flow/v3/node_modules
```
**Space recovered: ~1.9 GB**

---

## 🟡 Medium Priority — Worth Considering

### 3. Python Virtual Environments — ~370 MB
**Paths:**
- `SuperClaude_Framework/.venv/` — ~200 MB
- `Skill_Seekers/venv/` — ~165 MB

These can be recreated from `requirements.txt` or `pyproject.toml`. Safe to delete if those projects aren't in active use.

**Commands:**
```bash
rm -rf SuperClaude_Framework/.venv
rm -rf Skill_Seekers/venv
```
**Space recovered: ~370 MB**

---

### 4. ML Model Files — ~93 MB
**Path:** `claude-flow/v2/models/phi-4-mini/`

A downloaded ONNX model (`model.onnx` at 50 MB + `model.onnx.data` at 43 MB). Only needed if you're actively using the v2 local AI feature of Claude Flow.

**Space recovered: ~93 MB**

---

### 5. Large .git Pack File — 411 MB
**Path:** `claude-flow/.git/objects/pack/pack-11d490583c802b62...pack`

The git history for the claude-flow repo is stored in a single large pack file. Running `git gc --aggressive` can compress this further, or if you don't need the full history, you can do a shallow clone.

**Command (safe):** `cd claude-flow && git gc --aggressive --prune=now`  
**Space recovered: varies (10–50% reduction typically)**

---

## 🟢 Low Priority — Minor Cleanup

### 6. Duplicate Archive Files — ~15 MB
The following zip files appear twice (once in `claude-flow/node_modules/` and once in `claude-flow/v3/node_modules/`):
- `sql.js/dist/sqljs-all.zip` — 3.6 MB × 2
- `sqlite-autoconf-3440200.tar.gz` — 3.1 MB × 2
These will be removed automatically when you clean node_modules.

### 7. Daemon & Audit Log Files
Scattered `.claude-flow/logs/` folders contain daemon logs and audit logs. These are safe to delete.  
**Command:** `find . -path "*/.claude-flow/logs" -type d -exec rm -rf {} +`

---

## Recommended Cleanup Order

| Step | Action | Space Freed |
|------|--------|------------|
| 1 | Delete `.claude/worktrees/` | **1.4 GB** |
| 2 | Delete `claude-flow/node_modules` & `v3/node_modules` | **~1.9 GB** |
| 3 | Delete Python venvs | **~370 MB** |
| 4 | Delete phi-4-mini model | **~93 MB** |
| 5 | Run `git gc` on claude-flow | **~50–200 MB** |
| **Total** | | **~3.7 GB** |

---

## What's Safe to Keep

| Folder | Size | Notes |
|--------|------|-------|
| `Skill Library/` | 1.7 MB | Your custom skills — keep |
| `career-ops/` | 22 MB | Job search materials — keep |
| `interview-prep/` | 428 KB | Interview prep docs — keep |
| `docs/` | 128 KB | Documentation — keep |
| `src/` | 124 KB | Source code — keep |
| `scripts/` | 480 KB | Utility scripts — keep |
| `output/` | 5.7 MB | Mostly safe, review manually |

---

*This report is for your review — nothing has been deleted. Run the commands above when ready, or let me know which items you'd like help with.*
